<x-layouts.app title="Monitoramento" breadcrumbs="Operacao / Monitoramento">
    <section class="card"><div class="actions" style="justify-content:space-between;"><h2>Saude operacional</h2>@can('monitoring.run_diagnostics')<form method="post" action="{{ route('admin.monitoring.run') }}">@csrf<button class="btn" type="submit">Executar diagnostico</button></form>@endcan</div><div class="table-wrap"><table><thead><tr><th>Item</th><th>Status</th><th>Descricao</th><th>Detalhes</th></tr></thead><tbody>@foreach($items as $name => $item)<tr><td>{{ $name }}</td><td><span class="badge">{{ $item['status']->label() }}</span></td><td>{{ $item['message'] }}</td><td><code>{{ json_encode($item['details'], JSON_UNESCAPED_UNICODE) }}</code></td></tr>@endforeach</tbody></table></div><a class="btn ghost" href="{{ route('admin.monitoring.failed-jobs') }}">Jobs falhos</a></section>
</x-layouts.app>
