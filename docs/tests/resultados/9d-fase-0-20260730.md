# Etapa 9D — Fase 0 (estado inicial)

**Data:** 2026-07-30
**Executado por:** Claude Code, no servidor de producao (`contato.tars.art.br`)
**Roteiro:** `docs/tests/knowledge-base-manual-etapa-9d.md`, Fase 0

Os itens 0.1 a 0.3 sao verificaveis no servidor e foram executados. Os itens
0.4 e 0.5 dependem de login em navegador com perfil real e ficaram bloqueados
por falta de usuario nesses perfis.

## Resultado

| # | Acao | Esperado | Resultado |
| --- | --- | --- | --- |
| 0.1 | `knowledge:diagnose` | Recuperacao desligada, estrategia `lexical`, provedores listados | **Passou.** `knowledge.enabled = 0`, estrategia `lexical`, `top_k` 5, threshold 0.25, contexto 4000. Provedores de armazenamento e embeddings: `null`, nao configurados. |
| 0.2 | Antivirus disponivel | Se exigido e ausente, todo upload e recusado | **Passou com correcao.** ClamAV 1.4.3 presente e `knowledge.antivirus_required = 1`. A base de assinaturas estava em 10/abr/2026 e o `clamav-freshclam` estava inativo. Servico habilitado e iniciado; `daily.cvd` foi de 27967 para 28077. Verificacao ponta a ponta: arquivo limpo devolve `OK` com saida 0, arquivo EICAR devolve `Eicar-Signature FOUND` com saida 1. |
| 0.3 | Extrator de PDF | Se ausente, PDF falha com `extrator_pdf_indisponivel` | **Passou.** `poppler-utils` instalado; `pdftotext` 21.01.0 em `/bin/pdftotext`, que e o caminho esperado por `config/knowledge.php`. Homologacao de PDF liberada na Fase 2.9. |
| 0.4 | Entrar com perfil **consulta** | Menu mostra *Base de conhecimento*, nao mostra *Nova base* | **Bloqueado.** Nenhum usuario com o papel `consulta`. |
| 0.5 | Entrar com perfil **operador** | Envia documento; nao aprova nem baixa o original | **Bloqueado.** Nenhum usuario com o papel `operador`. |

## Estado do ambiente

- Migrations das etapas 9B, 9C e 9D aplicadas; nenhuma pendente.
- Seeders aplicados: 177 configuracoes, 8 permissoes `knowledge.*`.
- Worker `gerenciador-mensagens-queue` ativo com a fila `knowledge-indexing`.
- Usuarios: 2 administradores. Nenhum operador, nenhum perfil de consulta.
- Nenhum fluxo de conversa cadastrado.
- Nenhuma base de conhecimento cadastrada.
- Nenhuma credencial de provedor de IA configurada.

## Impedimentos para seguir

1. **Sem usuario `operador` e sem usuario `consulta`.** Bloqueia 0.4, 0.5, 4.2 e a
   separacao de responsabilidade que a etapa existe para provar: quem prepara a
   base nao e quem a aprova. Com um unico perfil nao ha o que homologar.
2. **Sem fluxo de conversa.** Bloqueia a Fase 1.3 (associar a base ao fluxo) e,
   antes disso, revela que a Etapa 9A nunca foi homologada em producao. O
   roteiro da 9D exige 9A, 9B e 9C homologadas como pre-requisito.
3. **Sem provedor de IA configurado.** Bloqueia a Fase 6, que e a unica que
   depende de provedor real. As Fases 1 a 5 rodam sem ele.
