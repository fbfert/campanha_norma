<x-layouts.app title="Detalhe da exportacao" breadcrumbs="Operacao / Exportacoes / Detalhe">
    <section class="card"><h2>{{ $export->report_type }}.{{ $export->format }}</h2><p>Status: {{ $export->status->label() }}</p><p>Linhas: {{ $export->total_rows }} | Tamanho: {{ $export->file_size ?? '-' }}</p><p>Expira em: {{ $export->expires_at?->format($dateTimeFormat) }}</p><p>Erro: {{ $export->error_code ? $export->error_code.' - '.$export->error_message : '-' }}</p><div class="actions">@if($export->status === \App\Enums\ReportExportStatus::Completed)<a class="btn" href="{{ route('admin.report-exports.download', $export) }}">Baixar</a>@endif @if($export->status === \App\Enums\ReportExportStatus::Failed)<form method="post" action="{{ route('admin.report-exports.retry', $export) }}">@csrf<button class="btn secondary" type="submit">Reprocessar</button></form>@endif</div></section>
</x-layouts.app>
